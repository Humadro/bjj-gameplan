# vitest@5 / vite@8 declaran un peerOptional de @types/node >=22; el proyecto
# fija @types/node@^20 (el que le toca a Next 16 + Node 20). Es opcional y no
# afecta al runtime, pero npm en Vercel aborta el install. Lo toleramos.
legacy-peer-deps=true
